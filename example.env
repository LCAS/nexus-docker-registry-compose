# Environment Configuration

This file contains environment variables for configuring the Nexus Docker Registry with authentication and zrok proxy settings.

## Variables

- `BASIC_AUTH_USER`: Username for basic authentication to access the registry
- `BASIC_AUTH_PASSWORD`: Password for basic authentication (should be a secure random string)
- `ZROK_ENV_NAME`: Environment name for the zrok proxy configuration 
- `ZROK_API_ENDPOINT`: API endpoint URL for the zrok service
- `ZROK_TOKEN`: Authentication token for accessing the zrok API (keep secret)
- `ZROK_JABAS_TOKEN`: Authentication token for the JABAS service integration (keep secret)
- `ZROK_JABAS_API_ENDPOINT`: API endpoint URL for the JABAS service

## Security Notes

- Replace all example tokens and passwords with actual secure values
- Ensure this file is not committed to version control
- Use strong, randomly generated passwords and tokens

BASIC_AUTH_USER=user
BASIC_AUTH_PASSWORD=SECRET

ZROK_ENV_NAME=nexus@lcas-proxy

ZROK_API_ENDPOINT=https://zrok.zrok.lcas.group
ZROK_TOKEN=SECRET

ZROK_JABAS_TOKEN=SECRET
ZROK_JABAS_API_ENDPOINT=https://zrok.services.jabas.ai/
